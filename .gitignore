node_modules/
.next/
.env.local
.env
*.log
.DS_Store
__pycache__/
*.pyc
.accounts.json
.deployed.json
*.tsbuildinfo

# Local agent/editor scratch files are excluded via .git/info/exclude,
# which is per-clone and not committed.
